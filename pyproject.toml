[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fba-ipo-underpricing-model"
version = "0.1.0"
description = "IPO underpricing prediction model"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["src*"]
